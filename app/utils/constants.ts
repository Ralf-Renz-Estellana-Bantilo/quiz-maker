import { Quiz } from '@/types';

export const DEFAULT_QUIZ_FORM_VALUE: Quiz = {
   createdAt: '',
   description: '',
   id: 0,
   isPublished: false,
   title: '',
   timeLimitSeconds: undefined,
};
